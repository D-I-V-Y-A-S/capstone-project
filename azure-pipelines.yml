trigger:
- master

pool:
  name: 'Default'

steps:
- task: JavaToolInstaller@0
  inputs:
    versionSpec: '17'
    jdkArchitectureOption: 'x64'
    jdkSourceOption: 'PreInstalled'
    jdkDistributions: 'OpenJDK'

- task: Maven@4
  inputs:
    mavenPomFile: 'demo/pom.xml'
    goals: 'package'
